services:
  pi-tree:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT:-3847}:${PORT:-3847}"
    env_file: .env
    volumes:
      - ~/repos/pi-tree-data:/data
      - ./config/mcp.json:/data/mcp.json:ro
    environment:
      - DATA_PATH=/data
