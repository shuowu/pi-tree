services:
  pi-tree:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT:-3847}:${PORT:-3847}"
    env_file: .env
    volumes:
      - pi-tree-data:/data
      - ./config/mcp.json:/data/mcp.json:ro
    environment:
      - DATA_PATH=/data

volumes:
  pi-tree-data:
