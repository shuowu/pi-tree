// Preload script — runs in renderer context before the web page.
// Currently empty — contextIsolation is enabled, nodeIntegration is off.
// Future: expose IPC bridge here for direct app.fetch() calls (no HTTP).
